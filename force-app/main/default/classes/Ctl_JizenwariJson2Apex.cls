//
// Generated by JSON2Apex http://json2apex.herokuapp.com/
//
public class Ctl_JizenwariJson2Apex {

    public String gaclient_id;
    public String last_name;
    public String first_name;
    public String last_name_kana;
    public String first_name_kana;
    public String address_post;      // 郵便番号
    public String address_state;     // 都道府県
    public String address_city;      // 市区町村
    public String address_street;    // 番地以降
    public String email;             // メールアドレス
    public String uniq_id;           // メール処理Id
    public String phone;             // 電話（取引先）

    public String sid;               // sId
    public String payment_status;    // 入金状況（ステータス）
    public String sogi_planHope;     //　お考えのプラン
    public String areaState;         // 【ご希望エリア】都道府県
    public String areaCity;          // 【ご希望エリア】市区町村など
    public String plan;              // 利用プラン
    public String payment_type;      // 支払い方法（新規）
    public String taisyosya_jokyo;   // 対象者の状態
    public String simpleJizenwariOwns_material;
    public String req_Day;           // 問合せ日

    public String customer_bango;      // ログイン取引先番号
    public String member_touroku_kibo; // メンバー登録希望  
    public String web_account_kibo;    // Webアカウント開設希望 


    public static Ctl_JizenwariJson2Apex parse(String json) {
        System.debug((Ctl_JizenwariJson2Apex) System.JSON.deserialize(json, Ctl_JizenwariJson2Apex.class));
        return (Ctl_JizenwariJson2Apex) System.JSON.deserialize(json, Ctl_JizenwariJson2Apex.class);
    }


    //　支払い方法
    public String getSeikyuHouhou(){

        String inputShiharaiHoho;

        System.debug('--payment_type--' + payment_type);
        if(this.payment_type == 'クレジットカード決済'){
          inputShiharaiHoho = 'paydesign_credit';
        }

        if(this.payment_type == 'コンビニ払い（コンビニ端末操作）'){
          inputShiharaiHoho = 'paydesign_cvs_tan';
        }

        if(this.payment_type == 'コンビニ払い（伝票払い）'){
          inputShiharaiHoho = 'paydesign_cvs_den';
        }
        System.debug('--inputShiharaiHoho--' + inputShiharaiHoho);

        return inputShiharaiHoho;
    }

    // フェーズ
    public String getStageName(){

        String stageName;
        if(this.payment_status == '事前割-申込中' || this.payment_status == '事前割-申込完了(伝票発送待ち)'){
            stageName = '申込受付';
        }
        return stageName;
    }



}